#include <stdio.h>
int main (){
	int a = 25;
	int b = 5;
	int tong = a + b;
	int hieu = a - b;
	int tich = a * b;
	int thuong = a / b;
	printf("tong 2 so la %d\n", tong);
	printf("hieu 2 so la %d\n", hieu);
	printf("tich 2 so la %d\n", tich);
	printf("thuong 2 so la %d\n", thuong);
	return 0;
}
